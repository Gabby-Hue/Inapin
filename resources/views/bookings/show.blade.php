<x-layout><h1 class="text-2xl font-bold">Booking #{{ $booking->id }}</h1><p>{{ $booking->property->name }}</p><p>{{ $booking->check_in->toDateString() }} - {{ $booking->check_out->toDateString() }} • {{ $booking->guest_count }} tamu</p><p>Status: {{ $booking->status }}</p><p>Total: Rp{{ number_format($booking->total_price) }}</p>@if($booking->status !== 'cancelled')<form method="post" action="{{ route('bookings.update',$booking) }}">@csrf @method('PUT')<input type="hidden" name="status" value="cancelled"><button class="mt-4 border px-3 py-2 rounded">Batalkan Booking</button></form>@endif</x-layout>
