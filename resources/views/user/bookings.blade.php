<x-layout><h1 class="text-2xl font-bold mb-4">Riwayat Booking</h1>@foreach($bookings as $booking)<div class="bg-white p-4 rounded mb-3"><a class="font-bold" href="{{ route('bookings.show',$booking) }}">{{ $booking->property->name }}</a><p>{{ $booking->check_in->toDateString() }} - {{ $booking->check_out->toDateString() }} • {{ $booking->status }} • Rp{{ number_format($booking->total_price) }}</p>@if($booking->status === 'completed' && ! $booking->review)<form method="post" action="{{ route('reviews.store') }}" class="grid gap-2 max-w-md mt-2">@csrf<input type="hidden" name="booking_id" value="{{ $booking->id }}"><input type="number" name="rating" min="1" max="5" class="border p-2" placeholder="Rating 1-5"><textarea name="comment" class="border p-2" placeholder="Komentar"></textarea><button class="bg-emerald-700 text-white p-2 rounded">Kirim Review</button></form>@endif</div>@endforeach</x-layout>
