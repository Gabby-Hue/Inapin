<x-layout><h1 class="text-2xl font-bold mb-4">Manage Flights</h1><form method="post" action="{{ route('admin.airports.store') }}" class="bg-white p-3 rounded mb-3 flex gap-2">@csrf<input name="name" placeholder="Bandara" class="border p-2"><input name="city" placeholder="Kota" class="border p-2"><input name="code" placeholder="Kode" class="border p-2"><button>Tambah Bandara</button></form><form method="post" action="{{ route('admin.flights.store') }}" class="bg-white p-3 rounded mb-3 grid gap-2 md:grid-cols-3">@csrf<input name="airline" placeholder="Maskapai" class="border p-2"><select name="origin_airport_id" class="border p-2">@foreach($airports as $airport)<option value="{{ $airport->id }}">{{ $airport->city }} ({{ $airport->code }})</option>@endforeach</select><select name="destination_airport_id" class="border p-2">@foreach($airports as $airport)<option value="{{ $airport->id }}">{{ $airport->city }} ({{ $airport->code }})</option>@endforeach</select><input type="datetime-local" name="departure_time" class="border p-2"><input type="datetime-local" name="arrival_time" class="border p-2"><input type="number" name="price" placeholder="Harga" class="border p-2"><button>Tambah Flight</button></form>@foreach($flights as $flight)<div class="bg-white p-3 rounded mb-2">{{ $flight->airline }} {{ $flight->originAirport->city }} → {{ $flight->destinationAirport->city }}</div>@endforeach</x-layout>
