<x-layout><h1 class="text-2xl font-bold mb-4">Partners</h1>@foreach($partners as $partner)<div class="bg-white p-4 rounded mb-3"><strong>{{ $partner->business_name }}</strong><p>{{ $partner->user->email }} • {{ $partner->verification_status }}</p><form method="post" action="{{ route('admin.partners.update',$partner) }}">@csrf @method('PUT')<select name="verification_status" class="border p-1">@foreach(['pending','approved','rejected'] as $status)<option @selected($partner->verification_status===$status)>{{ $status }}</option>@endforeach</select><button>Update</button></form></div>@endforeach</x-layout>
