<x-layout><h1 class="text-2xl font-bold mb-4">Reviews</h1>@foreach($reviews as $review)<div class="bg-white p-4 rounded mb-3"><strong>{{ $review->rating }}/5</strong> {{ $review->property->name }} oleh {{ $review->user->name }}<p>{{ $review->comment }}</p><form method="post" action="{{ route('admin.reviews.destroy',$review) }}">@csrf @method('DELETE')<button>Hapus</button></form></div>@endforeach</x-layout>
