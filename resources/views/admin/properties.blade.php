<x-layout><h1 class="text-2xl font-bold mb-4">Properties</h1>@foreach($properties as $property)<div class="bg-white p-4 rounded mb-3"><strong>{{ $property->name }}</strong><p>{{ $property->partner->business_name }} • {{ $property->city }} • {{ $property->status }}</p><form method="post" action="{{ route('admin.properties.update',$property) }}">@csrf @method('PUT')<select name="status" class="border p-1">@foreach(['pending','approved','rejected'] as $status)<option @selected($property->status===$status)>{{ $status }}</option>@endforeach</select><button>Update</button></form></div>@endforeach</x-layout>
