<x-layout><h1 class="text-2xl font-bold mb-4">Manage Ferries</h1><form method="post" action="{{ route('admin.ports.store') }}" class="bg-white p-3 rounded mb-3 flex gap-2">@csrf<input name="name" placeholder="Pelabuhan" class="border p-2"><input name="city" placeholder="Kota" class="border p-2"><button>Tambah Pelabuhan</button></form><form method="post" action="{{ route('admin.ferries.store') }}" class="bg-white p-3 rounded mb-3 grid gap-2 md:grid-cols-3">@csrf<input name="operator" placeholder="Operator" class="border p-2"><select name="origin_port_id" class="border p-2">@foreach($ports as $port)<option value="{{ $port->id }}">{{ $port->city }} - {{ $port->name }}</option>@endforeach</select><select name="destination_port_id" class="border p-2">@foreach($ports as $port)<option value="{{ $port->id }}">{{ $port->city }} - {{ $port->name }}</option>@endforeach</select><input type="datetime-local" name="departure_time" class="border p-2"><input type="datetime-local" name="arrival_time" class="border p-2"><input type="number" name="price" placeholder="Harga" class="border p-2"><button>Tambah Ferry</button></form>@foreach($ferries as $ferry)<div class="bg-white p-3 rounded mb-2">{{ $ferry->operator }} {{ $ferry->originPort->city }} → {{ $ferry->destinationPort->city }}</div>@endforeach</x-layout>
