<x-layout><h1 class="text-3xl font-bold">{{ $property->name }}</h1><p class="mt-2">{{ $property->category }} di {{ $property->city }} • Kapasitas {{ $property->capacity }} tamu</p><p class="mt-4">{{ $property->description }}</p><p class="mt-2 font-bold">Rp{{ number_format($property->price_per_night) }}/malam</p><p>Alamat: {{ $property->address }}</p><p>Rating rata-rata: {{ $property->average_rating ?? 'Belum ada' }}</p>@auth<form method="post" action="{{ route('favorites.store') }}" class="mt-4">@csrf<input type="hidden" name="property_id" value="{{ $property->id }}"><button class="border px-3 py-2 rounded">Tambah Favorit</button></form><form method="post" action="{{ route('bookings.store') }}" class="mt-6 grid gap-2 max-w-md bg-white p-4 rounded">@csrf<h2 class="font-bold">Booking</h2><input type="hidden" name="property_id" value="{{ $property->id }}"><input type="date" name="check_in" class="border p-2"><input type="date" name="check_out" class="border p-2"><input type="number" name="guest_count" min="1" placeholder="Jumlah tamu" class="border p-2"><button class="bg-emerald-700 text-white p-2 rounded">Buat Booking</button></form>@endauth<h2 class="text-xl font-bold mt-8">Review</h2>@foreach($property->reviews as $review)<div class="bg-white p-3 my-2 rounded"><strong>{{ $review->rating }}/5</strong> oleh {{ $review->user->name }}<p>{{ $review->comment }}</p></div>@endforeach</x-layout>
