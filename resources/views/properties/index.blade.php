<x-layout><h1 class="text-2xl font-bold mb-4">Penginapan</h1><form class="flex gap-2 mb-4"><input name="city" value="{{ request('city') }}" placeholder="Kota" class="border p-2"><select name="category" class="border p-2"><option value="">Semua kategori</option>@foreach($categories as $category)<option @selected(request('category')===$category)>{{ $category }}</option>@endforeach</select><button class="bg-emerald-700 text-white px-4 rounded">Cari</button></form><div class="grid md:grid-cols-3 gap-4">@foreach($properties as $property)<a class="bg-white p-4 rounded shadow" href="{{ route('properties.show',$property) }}"><h2 class="font-bold">{{ $property->name }}</h2><p>{{ $property->category }} • {{ $property->city }}</p><p>Rp{{ number_format($property->price_per_night) }}/malam</p><p>Rating: {{ $property->average_rating ?? 'Belum ada' }}</p></a>@endforeach</div>{{ $properties->links() }}</x-layout>
