<x-layout><h1 class="text-2xl font-bold mb-4">Rekomendasi penginapan di {{ $data['city'] }}</h1><div class="grid md:grid-cols-3 gap-4">@forelse($properties as $property)<a class="bg-white p-4 rounded shadow" href="{{ route('properties.show',$property) }}">{{ $property->name }} — {{ $property->category }}</a>@empty<p>Belum ada properti disetujui di destinasi ini.</p>@endforelse</div></x-layout>
