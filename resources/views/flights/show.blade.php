<x-layout><h1 class="text-2xl font-bold">{{ $flight->airline }}</h1><p>{{ $flight->originAirport->city }} → {{ $flight->destinationAirport->city }}</p><p>{{ $flight->departure_time }} - {{ $flight->arrival_time }} • Rp{{ number_format($flight->price) }}</p><h2 class="text-xl font-bold mt-6">Rekomendasi penginapan di {{ $flight->destinationAirport->city }}</h2>@foreach($properties as $property)<a class="block bg-white p-3 rounded my-2" href="{{ route('properties.show',$property) }}">{{ $property->name }}</a>@endforeach</x-layout>
