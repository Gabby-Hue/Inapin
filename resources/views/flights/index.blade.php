<x-layout><h1 class="text-2xl font-bold mb-4">Cari Penerbangan</h1><form action="{{ route('flights.search') }}" class="flex gap-2 mb-4"><input name="origin" value="{{ request('origin') }}" placeholder="Asal" class="border p-2"><input name="destination" value="{{ request('destination') }}" placeholder="Tujuan" class="border p-2"><button class="bg-emerald-700 text-white px-4 rounded">Cari</button></form>@foreach($flights as $flight)<a class="block bg-white p-4 rounded shadow mb-3" href="{{ route('flights.show',$flight) }}"><strong>{{ $flight->airline }}</strong> {{ $flight->originAirport->city }} ({{ $flight->originAirport->code }}) → {{ $flight->destinationAirport->city }} ({{ $flight->destinationAirport->code }}) Rp{{ number_format($flight->price) }}</a>@endforeach{{ $flights->links() }}</x-layout>
