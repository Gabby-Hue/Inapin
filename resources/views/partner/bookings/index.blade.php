<x-layout><h1 class="text-2xl font-bold mb-4">Booking Properti</h1>@foreach($bookings as $booking)<div class="bg-white p-4 rounded mb-3"><strong>{{ $booking->property->name }}</strong><p>{{ $booking->user->name }} • {{ $booking->status }} • Rp{{ number_format($booking->total_price) }}</p><form method="post" action="{{ route('partner.bookings.update',$booking) }}">@csrf @method('PUT')<select name="status" class="border p-1">@foreach(['pending','confirmed','completed','cancelled'] as $status)<option @selected($booking->status===$status)>{{ $status }}</option>@endforeach</select><button>Update</button></form></div>@endforeach</x-layout>
