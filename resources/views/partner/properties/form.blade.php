<form method="post" action="{{ $action }}" enctype="multipart/form-data" class="grid gap-3 max-w-2xl bg-white p-4 rounded">@csrf @if($method !== 'POST') @method($method) @endif
<input name="name" value="{{ old('name', $property?->name) }}" placeholder="Nama" class="border p-2"><textarea name="description" placeholder="Deskripsi" class="border p-2">{{ old('description', $property?->description) }}</textarea><select name="category" class="border p-2">@foreach($categories as $category)<option @selected(old('category', $property?->category)===$category)>{{ $category }}</option>@endforeach</select><input name="city" value="{{ old('city', $property?->city) }}" placeholder="Kota" class="border p-2"><input name="address" value="{{ old('address', $property?->address) }}" placeholder="Alamat" class="border p-2"><input name="price_per_night" type="number" value="{{ old('price_per_night', $property?->price_per_night) }}" placeholder="Harga per malam" class="border p-2"><input name="capacity" type="number" value="{{ old('capacity', $property?->capacity) }}" placeholder="Kapasitas" class="border p-2"><input name="facilities" value="{{ old('facilities', implode(', ', $property?->facilities ?? [])) }}" placeholder="Fasilitas, pisahkan koma" class="border p-2"><input type="file" name="images[]" multiple class="border p-2"><button class="bg-emerald-700 text-white p-2 rounded">Simpan</button></form>
