<x-layout><h1 class="text-2xl font-bold mb-4">Cari Jadwal Kapal</h1><form action="{{ route('ferries.search') }}" class="flex gap-2 mb-4"><input name="origin" value="{{ request('origin') }}" placeholder="Pelabuhan/Kota asal" class="border p-2"><input name="destination" value="{{ request('destination') }}" placeholder="Pelabuhan/Kota tujuan" class="border p-2"><button class="bg-emerald-700 text-white px-4 rounded">Cari</button></form>@foreach($ferries as $ferry)<a class="block bg-white p-4 rounded shadow mb-3" href="{{ route('ferries.show',$ferry) }}"><strong>{{ $ferry->operator }}</strong> {{ $ferry->originPort->city }} → {{ $ferry->destinationPort->city }} Rp{{ number_format($ferry->price) }}</a>@endforeach{{ $ferries->links() }}</x-layout>
