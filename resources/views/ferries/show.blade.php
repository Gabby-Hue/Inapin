<x-layout><h1 class="text-2xl font-bold">{{ $ferry->operator }}</h1><p>{{ $ferry->originPort->name }} → {{ $ferry->destinationPort->name }}</p><p>{{ $ferry->departure_time }} - {{ $ferry->arrival_time }} • Rp{{ number_format($ferry->price) }}</p><h2 class="text-xl font-bold mt-6">Rekomendasi penginapan di {{ $ferry->destinationPort->city }}</h2>@foreach($properties as $property)<a class="block bg-white p-3 rounded my-2" href="{{ route('properties.show',$property) }}">{{ $property->name }}</a>@endforeach</x-layout>
